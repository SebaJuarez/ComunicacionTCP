version: '3.8'

services:
  servidor:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "5000:5000"  # Exponer el puerto 5000 para la conexión TCP
    networks:
      - tcp-network

  cliente:
    build:
      context: .
      dockerfile: Dockerfile.client
    networks:
      - tcp-network
    environment:
      - DIRECCION_SERVIDOR=servidor  # Establecer el nombre del contenedor del servidor como variable de entorno

networks:
  tcp-network:
    driver: bridge
